/**
 * Dialog Hooks
 * 
 * useDialog - Simple dialog state management (open/close/toggle)
 * useConfirmDialog - Promise-based confirmation dialogs (async/await pattern)
 * 
 * Usage:
 * ```tsx
 * import { useDialog, useConfirmDialog } from '@/components/dialogs'
 * ```
 */

export { useDialog } from './use-dialog'
export { useConfirmDialog } from './use-confirm-dialog'
