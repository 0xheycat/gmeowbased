/**
 * Confirmation Dialog Components
 * 
 * Pre-built confirmation dialogs with variants:
 * - destructive (red): For dangerous/irreversible actions
 * - warning (yellow): For actions requiring caution
 * - info (blue): For informational confirmations
 * 
 * Usage:
 * ```tsx
 * import { ConfirmDialog } from '@/components/dialogs'
 * ```
 */

export { default as ConfirmDialog } from './confirm-dialog'
export type { ConfirmDialogProps, ConfirmDialogVariant } from './confirm-dialog'
