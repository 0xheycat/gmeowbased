# Phase 7 Priority 2: Redis Caching Layer
# Docker Compose configuration for Redis

version: '3.8'

services:
  redis:
    image: redis:7-alpine
    container_name: gmeow-redis
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    command: redis-server --appendonly yes --appendfsync everysec
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

volumes:
  redis-data:
    driver: local
